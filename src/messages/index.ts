import { prDescriptionMessages } from './prDescription';
import { commitMessages } from './commit';
import { raconteurMessages } from './raconteur';

export const messages = {
  prDescription: prDescriptionMessages,
  commit: commitMessages,
  raconteur: raconteurMessages,
};